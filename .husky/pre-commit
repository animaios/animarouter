# Only format staged files
staged_files=$(git diff --cached --name-only --diff-filter=ACM -- '*.ts' '*.tsx' '*.js' '*.jsx' '*.mjs' '*.cjs' '*.json')
if [ -n "$staged_files" ]; then
  npx biome check --write $staged_files
fi
