#!/usr/bin/env node
/**
 * proxy-up.mjs — Stub (removed: freellmproxy submodule was deleted).
 *
 * The freellmproxy git submodule and all references to it have been
 * removed from the monorepo. All proxy-related commands are no-ops.
 */
console.log(
  "ℹ️  proxy commands are not available (freellmproxy submodule removed).",
);
process.exit(0);
